services:
  mongo:
    image: mongo:7
    container_name: nexus-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - nexus-mongo-data:/data/db

volumes:
  nexus-mongo-data:
